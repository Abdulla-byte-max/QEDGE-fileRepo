Working in the modules
userreports
cerditcard
debitcard
transcation of users
# working wuith webhook job
card services
ticket raising
user actions and folleing transactions
         x = random.nextInt(BOARD_WIDTH);
            y = random.nextInt(BOARD_HEIGHT);
        } while (isSnakeAt(x, y));
        
        food = new Point(x, y);
    }
    
    private boolean isSnakeAt(int x, int y) {
        for (Point p : snake) {
            if (p.x == x && p.y == y) {
                return true;
            }
        }
        return false;
